/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['i.imgur.com'], // List allowed image domains (replace with your actual domains)
    },
  };
  
  export default nextConfig;